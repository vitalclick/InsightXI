# Runtime dependencies (mirror of pyproject [project.dependencies]).
# For dev/test extras use: pip install -e ".[dev]"
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic>=2.7.4
scikit-learn>=1.5.1
xgboost>=2.1.0
pandas>=2.2.2
numpy>=2.0.0
# Used directly to (de)serialize trained model artifacts in app.training.train
# and app.models.ml_ensemble. scikit-learn pulls it in transitively, but it is a
# direct dependency here, so pin it explicitly rather than rely on that.
joblib>=1.4.2
